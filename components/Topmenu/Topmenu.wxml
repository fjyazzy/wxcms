<!--components/Topmenu/Topmenu.wxml-->
<view class="page-section">
    <movable-area>
      <movable-view direction="horizontal">{{uname}}-
      <navigator wx:for="{{TopMenuList}}" wx:key="index" url="{{item.pagePath}}">{{item.text}}-</navigator>
      </movable-view>
    </movable-area>
</view>